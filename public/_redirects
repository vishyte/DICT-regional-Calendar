# Redirect rules for SPA routing on static hosting
# This ensures all routes serve index.html for client-side routing

# API calls should not be redirected
/api/*  https://dict-calendar-backend.onrender.com/api/:splat  200

# All other routes should serve index.html
/*    /index.html   200
